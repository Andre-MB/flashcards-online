version: '3.9'

services:
  angular:
    image: node:20
    working_dir: /app
    volumes:
      - .:/app
    ports:
      - '4200:4200'
    command: sh -c "npm install && npx ng serve --host 0.0.0.0"
